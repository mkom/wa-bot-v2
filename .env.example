# WhatsApp Bot Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# Upstash Redis Configuration
# ===========================================
# Get these from https://console.upstash.com/
# Select your database and copy the REST credentials
UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token_here

# ===========================================
# Vercel Environment Variables
# ===========================================
# These should be set in Vercel Dashboard:
# Project Settings > Environment Variables
#
# For production, add these variables in Vercel:
# - UPSTASH_REDIS_REST_URL (mark as Secret)
# - UPSTASH_REDIS_REST_TOKEN (mark as Secret)
# - CRON_SECRET (mark as Secret)

# ===========================================
# Application Configuration
# ===========================================
NODE_ENV=development
PORT=3000

# ===========================================
# WhatsApp Bot Configuration
# ===========================================
# Optional: Default bot settings
# WHATSAPP_NUMBER=1234567890
# AUTO_REPLY_ENABLED=false
# DEFAULT_PREFIX=!

# ===========================================
# CORS Configuration
# ===========================================
# Add your frontend URLs here (comma-separated)
# CORS_ORIGINS=https://your-frontend.vercel.app,http://localhost:3000

# ===========================================
# Rate Limiting
# ===========================================
# Rate limit settings (requests per time window)
# RATE_LIMIT_REQUESTS=10
# RATE_LIMIT_WINDOW=10s

# ===========================================
# Queue Configuration
# ===========================================
# Maximum number of jobs to process per cron run
MAX_JOBS_PER_RUN=10

# ===========================================
# Cron Job Configuration
# ===========================================
# Secret to protect cron endpoints (recommended for production)
# Generate a random string: openssl rand -base64 32
CRON_SECRET=your_cron_secret_here

# ===========================================
# Pipedream Integration (Optional)
# ===========================================
# For hybrid mode: Vercel (API/Queue) + Pipedream (Bot Processing)
# Get webhook URL from Pipedream workflow HTTP trigger
# Set to empty to use Vercel-only processing
PIPEDREAM_WEBHOOK_URL=

# ===========================================
# MongoDB (Optional - for persistent storage)
# ===========================================
# MONGODB_URI=mongodb://localhost:27017/wa-bot
